# Copilot Instructions

## Project Guidelines
- In PathHelper.GetRelativePart, the input path is a full absolute path with drive letter, and the expected output is that same path without the drive/root prefix.